import express from "express";
import authMiddleware from "../middleware/authMiddleware.js";
import pool from "../src/db.js";

const router = express.Router();

//* Fetch weights

//* Add weight entry

//* Remove/edit weight entry
