export enum Environments {
  Prod = "production",
  Local = "local",
  Dev = "development",
}

export enum Routes {
  Dashboard = "dashboard",
}

export enum InputTypes {
  Text = "text",
  Password = "password",
  Number = "number",
}
